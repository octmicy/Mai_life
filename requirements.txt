
playwright>=1.49,<2

# 增强依赖统一声明：缺失时代码有降级守卫（环境/菜单自动降级），
# 但安装失败不应由插件文件本身来兜底——由部署环境保证装齐。
chinese-calendar>=1.0,<2  # 中国工作日、法定节假日与调休
lunar-python>=1.4,<2      # 农历与节气
Pillow>=10.0,<12          # 本地指令菜单；缺失时降级为纯文本
